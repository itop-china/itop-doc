---
title: "变更管理模块"
linkTitle: "变更管理模块"
date: 2025-06-27
weight: 10
description: "iTop 变更管理模块，用于记录和跟踪 IT 环境中的计划变更，包括补丁安装、系统配置更改、操作系统更新和软件安装。"
---

{{% pageinfo %}}
本文档介绍 iTop 变更管理模块。更多详细内容请参考 [iTop 官方文档](https://www.itophub.io/wiki/page?id=3_2_0:datamodel:itop-change-mgmt)。
{{% /pageinfo %}}

## 概述

变更工单（简称变更）用于记录 IT 环境中计划进行的修改：

- 补丁安装
- 系统配置更改
- 操作系统更新
- 软件安装

通过记录所有 IT 修改，您可以轻松追踪变更历史。许多事件都是由 IT 环境变更引起的，记录变更有助于在事件发生时快速识别并恢复服务。

此外，变更管理模块可以自动分析变更对基础设施和应用解决方案的影响。IT 工程师可以更好地控制关键服务的不可用性，提高客户满意度。

变更工单由以下角色管理：

- **变更实施人**：计划和实施变更
- **变更主管**：跟踪变更进展
- **变更经理**：审批变更

安装 iTop 时，您可以在两个变更管理模块中选择。本文档介绍的模块采用简单的工作流，便于管理变更。

## 变更属性

| 名称 | 类型 | 必填？ |
|------|------|--------|
| 编号 | 字母数字字符串 | 是 |
| 组织 | 外键指向组织 | 是 |
| 状态 | 可选值：已批准、已分配、已关闭、新建、已计划、已拒绝 | 否 |
| 标题 | 字母数字字符串 | 是 |
| 描述 | 多行字符字符串 | 是 |
| 请求人 | 外键指向人员 | 是 |
| 团队 | 外键指向团队 | 否 |
| 代理 | 外键指向人员 | 否 |
| 变更经理 | 外键指向人员 | 否 |
| 类别 | 可选值：应用、硬件、网络、其他、软件、系统 | 否 |
| 拒绝原因 | 多行字符字符串 | 否 |
| 回退计划 | 多行字符字符串 | 否 |
| 父变更 | 外键指向变更 | 否 |
| 创建日期 | 日期时间（年-月-日 时:分:秒） | 否 |
| 开始日期 | 日期时间（年-月-日 时:分:秒） | 否 |
| 结束日期 | 日期时间（年-月-日 时:分:秒） | 否 |
| 最后更新 | 日期时间（年-月-日 时:分:秒） | 否 |
| 批准日期 | 日期时间（年-月-日 时:分:秒） | 否 |
| 关闭日期 | 日期时间（年-月-日 时:分:秒） | 否 |
| 中断 | 可选值：是、否 | 否 |

## 标签页

| 标签页 | 描述 |
|--------|------|
| 配置项 | 受此工单影响的所有配置项 |
| 联系人 | 与此工单关联的所有联系人 |
| 工作指令 | 此工单的所有工作指令 |
| 相关请求 | 与此变更关联的所有用户请求 |
| 相关问题 | 与此变更关联的所有问题 |
| 子变更 | 与此变更关联的所有子变更 |

## 创建变更

点击“新建变更”菜单，将显示以下表单。

## 将变更分配给团队和代理

可将变更分配的团队列表由对应组织的交付模型定义。创建变更时，代理需选择客户组织，然后团队列表将严格限定为该客户定义的团队。如果缺少某个团队，需要更新客户的交付模型以反映此需求。更多信息请参阅交付模型相关文档。

## 管理私有日志

变更工单只有私有日志，用于记录与其相关的所有活动和通信。私有日志在终端用户门户中不可见。

## 管理受影响的配置项和联系人

此部分与帮助台模块类似，请参阅相关文档。

## 变更生命周期

变更对象具有以下生命周期：

根据对象状态，属性约束如下表所示：

|  | 新建 | 已分配 | 已计划 | 已拒绝 | 已批准 | 已关闭 |
|------|------|--------|--------|--------|--------|--------|
| 编号 | 只读 | 只读 | 只读 | 只读 | 只读 | 只读 |
| 组织 | 必填 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 状态 | 只读 | 只读 | 只读 | 只读 | 只读 | 只读 |
| 标题 | 必填 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 描述 | 必填 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 请求人 | 必填 |  | 必填 | 只读 | 只读 | 只读 |
| 团队 | 隐藏 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 代理 | 隐藏 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 变更经理 | 隐藏 | 必填 | 必填 | 只读 | 只读 | 只读 |
| 类别 |  |  |  | 只读 |  | 只读 |
| 拒绝原因 | 隐藏 | 隐藏 | 隐藏 | 必填 | 只读 | 只读 |
| 回退计划 |  |  | 必填 | 只读 |  | 只读 |
| 父变更 |  |  |  | 只读 |  | 只读 |
| 创建日期 | 只读 | 只读 | 只读 | 只读 | 隐藏 | 只读 |
| 开始日期 |  |  | 必填 | 只读 | 只读 | 只读 |
| 结束日期 |  |  | 必填 | 只读 | 只读 | 只读 |
| 最后更新 | 只读 | 只读 | 只读 | 只读 | 只读 | 只读 |
| 批准日期 | 隐藏 | 隐藏 | 隐藏 | 只读 | 只读 | 只读 |
| 关闭日期 | 隐藏 | 隐藏 | 隐藏 | 隐藏 | 隐藏 | 只读 |
| 中断 | 隐藏 | 隐藏 | 必填 | 只读 | 只读 | 只读 |

- H：隐藏
- R/O：只读
- M：必填

---
原文：<https://www.itophub.io/wiki/page?id=3_2_0:datamodel:itop-change-mgmt>

版本：3_2_0/datamodel/itop-change-mgmt.txt · Last modified: 2024/09/10 10:25 by 127.0.0.1
